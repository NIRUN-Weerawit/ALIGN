# ALIGN — pip requirements
# Install: pip install -r requirements.txt
# For GPU support, use: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# Recommended approach:
#   pip install -r requirements.txt
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   python scripts/check_deps.py

# ── Core ML & data ──────────────────────────────────────────────
numpy
scipy
h5py
Pillow
open-clip-torch
lerobot
xformers
transformers
PyAV

# ── Monitoring ──────────────────────────────────────────────────
wandb
tqdm

# ── Utilities ───────────────────────────────────────────────────
requests

# ── Optional: Isaac Sim data collection ─────────────────────────
# Install Isaac Sim separately (pip install isaacsim or NVIDIA's installer)
# paho-mqtt        # MQTT for VR teleop
# tensorflow-datasets  # BridgeData v2 adapter