# ALIGN — pip requirements
# Install sequence (verified on both local and helios machines):
#
#   # Step 1: PyTorch + torchcodec (all from same CUDA index)
#   pip install torch==2.10.0 torchvision==0.25.0 torchcodec==0.10.0 \
#       --index-url https://download.pytorch.org/whl/cu128
#
#   # Step 2: xformers (separately — avoids dependency conflicts with torchvision)
#   pip install xformers --index-url https://download.pytorch.org/whl/cu128
#
#   # Step 3: Everything else
#   pip install -r requirements.txt
#
# Other CUDA versions (cu118, cu121, cu124) also work — just match the index URL.
# See https://pytorch.org/get-started/locally/ for available combos.

# ── Core ML & data ──────────────────────────────────────────────
numpy
scipy
h5py
Pillow
open-clip-torch
lerobot
transformers
PyAV

# ── Monitoring ──────────────────────────────────────────────────
wandb
tqdm

# ── Utilities ───────────────────────────────────────────────────
requests

# ── Optional: Isaac Sim data collection ─────────────────────────
# Install Isaac Sim separately (pip install isaacsim or NVIDIA's installer)
# paho-mqtt        # MQTT for VR teleop
# tensorflow-datasets  # BridgeData v2 adapter